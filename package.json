{
  "name": "random-anime-generator",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start-frontend\" \"npm run start-backend\"",
    "start-frontend": "cd \"frontend\" && npm start",
    "start-backend": "cd \"backend\" && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "i-all": "npm install && concurrently \"npm --prefix frontend install\" \"npm --prefix backend install\"",
    "install-all": "npm install && concurrently \"npm --prefix frontend install\" \"npm --prefix backend install\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
